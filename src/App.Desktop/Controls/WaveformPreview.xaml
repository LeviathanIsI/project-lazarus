<UserControl x:Class="Lazarus.Desktop.Controls.WaveformPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <Border Background="#121216" CornerRadius="6" BorderBrush="#2A2A2E" BorderThickness="1">
      <Grid>
        <ScrollViewer x:Name="ZoomHost" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
          <Grid>
            <Image x:Name="Image" RenderOptions.BitmapScalingMode="HighQuality"/>
            <Canvas x:Name="Overlay" Background="Transparent">
              <Rectangle x:Name="SelectionRect" Fill="#404D7A" Opacity="0.35" Visibility="Collapsed"/>
            </Canvas>
          </Grid>
        </ScrollViewer>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="6">
          <Button Width="28" Height="24" Content="+" Click="OnZoomIn"/>
          <Button Width="28" Height="24" Content="-" Margin="4,0,0,0" Click="OnZoomOut"/>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</UserControl>
