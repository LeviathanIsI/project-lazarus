<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ========================================
         NAVIGATION SPECIFIC STYLES
         Theme-aware navigation sidebar components
         ======================================== -->

    <!-- Navigation Section Header Style - Prominent main section labels -->
    <Style x:Key="NavigationSectionHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
        <Setter Property="Margin" Value="16,24,16,8" />
        <Setter Property="Opacity" Value="1" />
        <Setter Property="IsHitTestVisible" Value="False" />
    </Style>

    <!-- Navigation Item Button Base Style - Subtle, secondary navigation items -->
    <Style x:Key="NavigationItemBaseStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="24,8,16,8" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Foreground" Value="{DynamicResource SecondaryForegroundBrush}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0">
                        <Grid>
                            <Border Name="LeftAccent" 
                                    Width="3" 
                                    HorizontalAlignment="Left" 
                                    Background="Transparent" />
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            Margin="{TemplateBinding Padding}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource HoverBackgroundBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource PressedBackgroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Navigation Item Style (Normal State) - Subtle hover states -->
    <Style x:Key="NavigationItemStyle" TargetType="Button" BasedOn="{StaticResource NavigationItemBaseStyle}">
        <Setter Property="Margin" Value="0,0,0,1" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource SurfaceBackgroundBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- NavigationItemSelectedStyle removed - defined per-theme in theme files -->

    <!-- Navigation Separator Style -->
    <Style x:Key="NavigationSeparatorStyle" TargetType="Rectangle">
        <Setter Property="Height" Value="1" />
        <Setter Property="Fill" Value="{DynamicResource BorderBrush}" />
        <Setter Property="Margin" Value="16,12,16,0" />
        <Setter Property="Opacity" Value="0.2" />
    </Style>
    
    <!-- Navigation Section Spacing Style - Creates visual breaks between sections -->
    <Style x:Key="NavigationSectionSpacerStyle" TargetType="Border">
        <Setter Property="Height" Value="16" />
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <!-- Navigation Panel Style -->
    <Style x:Key="NavigationPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,0" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="Width" Value="240" />
    </Style>

    <!-- Content Area Style -->
    <Style x:Key="ContentAreaStyle" TargetType="ContentPresenter">
        <Setter Property="Margin" Value="0" />
    </Style>

    <!-- Navigation Logo Style -->
    <Style x:Key="NavigationLogoStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource PrimaryBackgroundBrush}" />
        <Setter Property="Padding" Value="16,12" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
    </Style>

    <!-- Theme-specific hover backgrounds (fallbacks) -->
    <SolidColorBrush x:Key="HoverBackgroundBrush" Color="#404040" />
    <SolidColorBrush x:Key="PressedBackgroundBrush" Color="#505050" />

    <!-- Navigation Content Template for View Switching -->
    <DataTemplate x:Key="NavigationContentTemplate">
        <ContentPresenter Content="{Binding}" />
    </DataTemplate>

    <!-- Animation for smooth transitions -->
    <Storyboard x:Key="NavigationTransitionIn">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.2">
            <DoubleAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <ThicknessAnimation Storyboard.TargetProperty="Margin"
                            From="20,0,0,0" To="0,0,0,0" Duration="0:0:0.2">
            <ThicknessAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseOut"/>
            </ThicknessAnimation.EasingFunction>
        </ThicknessAnimation>
    </Storyboard>

</ResourceDictionary>