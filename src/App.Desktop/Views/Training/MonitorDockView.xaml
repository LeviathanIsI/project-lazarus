<UserControl x:Class="Lazarus.Desktop.Views.Training.MonitorDockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <TabControl>
        <TabItem Header="Metrics">
            <Grid Margin="8">
                <!-- TODO(training): charts placeholder -->
                <TextBlock Text="Live metrics (loss, accuracy, lr, step/epoch)"/>
            </Grid>
        </TabItem>
        <TabItem Header="Logs">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8">
                    <TextBox Width="280" Text="{Binding LogsFilter}" Style="{StaticResource GlassTextBoxStyle}"/>
                    <CheckBox Content="Autoscroll" IsChecked="{Binding Autoscroll}" Margin="8,0,0,0" Style="{StaticResource GlassCheckBoxStyle}"/>
                </StackPanel>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Logs}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontFamily="{StaticResource MonoFont}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>
        </TabItem>
        <TabItem Header="System">
            <Grid Margin="8">
                <!-- TODO(training): gauges placeholder -->
                <TextBlock Text="VRAM/CPU/Disk/IO gauges"/>
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>

