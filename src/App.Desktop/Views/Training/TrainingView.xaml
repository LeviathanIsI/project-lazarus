<UserControl x:Class="Lazarus.Desktop.Views.Training.TrainingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:Lazarus.Desktop.Views.Training"
             mc:Ignorable="d">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.InputBindings>
            <!-- Global keyboard shortcuts -->
            <KeyBinding Key="N" Modifiers="Ctrl" Command="{Binding JobsSidebar.NewJobCommand}"/>
            <KeyBinding Key="I" Modifiers="Ctrl" Command="{Binding JobsSidebar.ImportDatasetCommand}"/>
            <KeyBinding Key="Enter" Modifiers="Ctrl" Command="{Binding StartCommand}"/>
            <KeyBinding Key="P" Modifiers="Ctrl" Command="{Binding PauseCommand}"/>
            <KeyBinding Key="OemPeriod" Modifiers="Ctrl" Command="{Binding StopCommand}"/>
            <KeyBinding Key="OemTilde" Modifiers="Ctrl" Command="{Binding ToggleMonitorDockCommand}"/>
        </Grid.InputBindings>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top modality switch -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="16,12,16,8">
            <ToggleButton Style="{StaticResource GlassToggleButtonStyle}" Content="Conversations" IsChecked="{Binding SelectedModality, Converter={StaticResource StringToBooleanConverter}, ConverterParameter=Conversations}" Margin="0,0,8,0"/>
            <ToggleButton Style="{StaticResource GlassToggleButtonStyle}" Content="Voice" IsChecked="{Binding SelectedModality, Converter={StaticResource StringToBooleanConverter}, ConverterParameter=Voice}" Margin="0,0,8,0"/>
            <ToggleButton Style="{StaticResource GlassToggleButtonStyle}" Content="Images" IsChecked="{Binding SelectedModality, Converter={StaticResource StringToBooleanConverter}, ConverterParameter=Images}" Margin="0,0,8,0"/>
            <ToggleButton Style="{StaticResource GlassToggleButtonStyle}" Content="3D Models" IsChecked="{Binding SelectedModality, Converter={StaticResource StringToBooleanConverter}, ConverterParameter=ThreeD}" Margin="0,0,8,0"/>
            <ToggleButton Style="{StaticResource GlassToggleButtonStyle}" Content="Entities" IsChecked="{Binding SelectedModality, Converter={StaticResource StringToBooleanConverter}, ConverterParameter=Entities}" Margin="0,0,8,0"/>
            <ToggleButton Style="{StaticResource GlassToggleButtonStyle}" Content="Videos" IsChecked="{Binding SelectedModality, Converter={StaticResource StringToBooleanConverter}, ConverterParameter=Videos}"/>
            <ToggleButton Content="Design  Progress" Style="{StaticResource GlassToggleButtonStyle}" Margin="16,0,0,0" IsChecked="{Binding IsProgressMode}" ToolTip="Toggle designer vs progress full-screen"/>
        </StackPanel>

        <!-- Main regions -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <!-- Left Sidebar -->
            <Border Grid.Column="0" Style="{StaticResource GlassPanel}" Padding="8">
                <ContentControl>
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedModality}" Value="Conversations">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <DockPanel>
                                                <TextBlock DockPanel.Dock="Top" Text="Conversation Models" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                                <views:JobsSidebarView DataContext="{Binding JobsSidebar}"/>
                                            </DockPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SelectedModality}" Value="Voice">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <DockPanel>
                                                <TextBlock DockPanel.Dock="Top" Text="Voice Models" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                                <TextBlock Text="🎙️ Voice model training coming soon" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource SecondaryTextStyle}"/>
                                            </DockPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SelectedModality}" Value="Images">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <DockPanel>
                                                <TextBlock DockPanel.Dock="Top" Text="Image Models" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                                <views:JobsSidebarView DataContext="{Binding JobsSidebar}"/>
                                            </DockPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SelectedModality}" Value="ThreeD">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <DockPanel>
                                                <TextBlock DockPanel.Dock="Top" Text="3D Models" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                                <TextBlock Text="🗿 3D model training coming soon" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource SecondaryTextStyle}"/>
                                            </DockPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SelectedModality}" Value="Entities">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <DockPanel>
                                                <TextBlock DockPanel.Dock="Top" Text="Entity Models" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                                <TextBlock Text="🏷️ Entity model training coming soon" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource SecondaryTextStyle}"/>
                                            </DockPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SelectedModality}" Value="Videos">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <DockPanel>
                                                <TextBlock DockPanel.Dock="Top" Text="Video Models" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                                <TextBlock Text="🎬 Video model training coming soon" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource SecondaryTextStyle}"/>
                                            </DockPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                            <!-- Default content -->
                            <Setter Property="Content">
                                <Setter.Value>
                                    <DockPanel>
                                        <TextBlock DockPanel.Dock="Top" Text="Conversation Models" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                        <views:JobsSidebarView DataContext="{Binding JobsSidebar}"/>
                                    </DockPanel>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>
            </Border>

            <!-- Center Designer or Progress -->
            <Grid Grid.Column="2">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsProgressMode}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Border Style="{StaticResource GlassPanelElevated}" Padding="8">
                    <!-- Modality-specific content -->
                    <ContentControl>
                        <ContentControl.Style>
                            <Style TargetType="ContentControl">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedModality}" Value="Conversations">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <StackPanel>
                                                    <TextBlock Text="Conversations Training" Style="{StaticResource H2TextStyle}" Margin="0,0,0,16"/>
                                                    <TextBlock Text="Configure conversation/chat model training settings, datasets, and parameters." TextWrapping="Wrap" Margin="0,0,0,8"/>
                                                    <views:JobDesignerView DataContext="{Binding JobDesigner}"/>
                                                </StackPanel>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedModality}" Value="Voice">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <StackPanel>
                                                    <TextBlock Text="Voice Training" Style="{StaticResource H2TextStyle}" Margin="0,0,0,16"/>
                                                    <TextBlock Text="Train voice synthesis, recognition, or audio processing models." TextWrapping="Wrap" Margin="0,0,0,8"/>
                                                    <TextBlock Text="🎙️ Audio Training Features Coming Soon" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource SecondaryTextStyle}"/>
                                                </StackPanel>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedModality}" Value="Images">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <StackPanel>
                                                    <TextBlock Text="Image Training" Style="{StaticResource H2TextStyle}" Margin="0,0,0,16"/>
                                                    <TextBlock Text="Train image generation, classification, or processing models." TextWrapping="Wrap" Margin="0,0,0,8"/>
                                                    <views:JobDesignerView DataContext="{Binding JobDesigner}"/>
                                                </StackPanel>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedModality}" Value="ThreeD">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <StackPanel>
                                                    <TextBlock Text="3D Models Training" Style="{StaticResource H2TextStyle}" Margin="0,0,0,16"/>
                                                    <TextBlock Text="Train 3D model generation, mesh processing, or spatial understanding models." TextWrapping="Wrap" Margin="0,0,0,8"/>
                                                    <TextBlock Text="🗿 3D Training Features Coming Soon" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource SecondaryTextStyle}"/>
                                                </StackPanel>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedModality}" Value="Entities">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <StackPanel>
                                                    <TextBlock Text="Entities Training" Style="{StaticResource H2TextStyle}" Margin="0,0,0,16"/>
                                                    <TextBlock Text="Train named entity recognition, relationship extraction, or knowledge graph models." TextWrapping="Wrap" Margin="0,0,0,8"/>
                                                    <TextBlock Text="🏷️ Entity Training Features Coming Soon" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource SecondaryTextStyle}"/>
                                                </StackPanel>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedModality}" Value="Videos">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <StackPanel>
                                                    <TextBlock Text="Video Training" Style="{StaticResource H2TextStyle}" Margin="0,0,0,16"/>
                                                    <TextBlock Text="Train video generation, analysis, or motion understanding models." TextWrapping="Wrap" Margin="0,0,0,8"/>
                                                    <TextBlock Text="🎬 Video Training Features Coming Soon" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource SecondaryTextStyle}"/>
                                                </StackPanel>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                                <!-- Default content for Conversations -->
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <StackPanel>
                                            <TextBlock Text="Conversations Training" Style="{StaticResource H2TextStyle}" Margin="0,0,0,16"/>
                                            <TextBlock Text="Configure conversation/chat model training settings, datasets, and parameters." TextWrapping="Wrap" Margin="0,0,0,8"/>
                                            <views:JobDesignerView DataContext="{Binding JobDesigner}"/>
                                        </StackPanel>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>
                </Border>
            </Grid>
            <!-- Full-screen Progress Surface (visible when IsProgressMode) -->
            <Grid Grid.Column="2" Visibility="Collapsed">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsProgressMode}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Border Style="{StaticResource GlassPanelElevated}" Padding="8">
                    <!-- TODO(training): replace placeholder with charts+logs fullscreen composite -->
                    <TextBlock Text="Progress Mode — Charts &amp; Logs" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>

            <!-- Right Inspector -->
            <Border Grid.Column="4" Style="{StaticResource GlassPanel}" Padding="8">
                <ContentControl>
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedModality}" Value="Conversations">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <StackPanel>
                                                <TextBlock Text="Conversation Details" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                                <views:InspectorView DataContext="{Binding Inspector}"/>
                                            </StackPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SelectedModality}" Value="Voice">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <StackPanel>
                                                <TextBlock Text="Voice Details" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                                <TextBlock Text="Voice training details will be shown here when available." TextWrapping="Wrap" Style="{StaticResource SecondaryTextStyle}"/>
                                            </StackPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SelectedModality}" Value="Images">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <StackPanel>
                                                <TextBlock Text="Image Details" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                                <views:InspectorView DataContext="{Binding Inspector}"/>
                                            </StackPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SelectedModality}" Value="ThreeD">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <StackPanel>
                                                <TextBlock Text="3D Model Details" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                                <TextBlock Text="3D model training details will be shown here when available." TextWrapping="Wrap" Style="{StaticResource SecondaryTextStyle}"/>
                                            </StackPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SelectedModality}" Value="Entities">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <StackPanel>
                                                <TextBlock Text="Entity Details" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                                <TextBlock Text="Entity training details will be shown here when available." TextWrapping="Wrap" Style="{StaticResource SecondaryTextStyle}"/>
                                            </StackPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SelectedModality}" Value="Videos">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <StackPanel>
                                                <TextBlock Text="Video Details" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                                <TextBlock Text="Video training details will be shown here when available." TextWrapping="Wrap" Style="{StaticResource SecondaryTextStyle}"/>
                                            </StackPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                            <!-- Default content -->
                            <Setter Property="Content">
                                <Setter.Value>
                                    <StackPanel>
                                        <TextBlock Text="Conversation Details" Style="{StaticResource H3TextStyle}" Margin="0,0,0,8"/>
                                        <views:InspectorView DataContext="{Binding Inspector}"/>
                                    </StackPanel>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>
            </Border>
        </Grid>

        <!-- Bottom monitor dock -->
        <Grid Grid.Row="2" Margin="16,8,16,16" Visibility="Collapsed">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsMonitorOpen}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Border Style="{StaticResource GlassPanel}" Padding="8">
                <views:MonitorDockView DataContext="{Binding MonitorDock}"/>
            </Border>
        </Grid>

        <!-- Global action row (floats above dock) -->
        <DockPanel Grid.Row="2" Margin="16,0,16,8" VerticalAlignment="Bottom">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Start"   
                        Command="{Binding StartCommand}"   
                        IsEnabled="{Binding CanStart}"   
                        Style="{StaticResource RainbowAccentButton.AutoFit}" 
                        Margin="0,0,8,0"
                        ToolTip="Start selected training jobs (Ctrl+Enter)"/>
                <Button Content="Pause"   
                        Command="{Binding PauseCommand}"   
                        IsEnabled="{Binding CanPause}"   
                        Style="{StaticResource SolidDarkButton}" 
                        Margin="0,0,8,0"
                        ToolTip="Pause running jobs (Ctrl+P)"/>
                <Button Content="Stop"    
                        Command="{Binding StopCommand}"    
                        IsEnabled="{Binding CanStop}"    
                        Style="{StaticResource SolidDarkButton}" 
                        Margin="0,0,8,0"
                        ToolTip="Stop running/paused jobs (Ctrl+.)"/>
                <Button Content="Export"  
                        Command="{Binding ExportCommand}"  
                        IsEnabled="{Binding CanExport}"  
                        Style="{StaticResource SolidDarkButton}"
                        ToolTip="Export completed job results"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>
