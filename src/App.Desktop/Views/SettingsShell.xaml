<UserControl x:Class="Lazarus.Desktop.Views.SettingsShell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Lazarus.Desktop.ViewModels"
             mc:Ignorable="d">
    <DockPanel>
        <StackPanel DockPanel.Dock="Left"
                    Width="220"
                    Background="{StaticResource CardBackgroundBrush}">
            <TextBlock Text="Settings"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="16,24,16,16"/>
            <ListBox ItemsSource="{Binding Sections}"
                     SelectedItem="{Binding SelectedSectionVm, Mode=TwoWay}"
                     BorderThickness="0"
                     Background="Transparent"
                     Foreground="{StaticResource TextPrimaryBrush}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Title}" Padding="12,6"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <Border Background="{StaticResource GlassCardBrush}" Padding="24" BorderThickness="1" BorderBrush="{StaticResource GlassBorderBrush}" CornerRadius="8">
            <ContentControl x:Name="SettingsContent" Content="{Binding SelectedSectionVm, PresentationTraceSources.TraceLevel=High}">
                <ContentControl.ContentTemplate>
                    <!-- Template supplied in App resources via DataTemplates for section VMs -->
                    <DataTemplate>
                        <TextBlock Text="No content" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </DataTemplate>
                </ContentControl.ContentTemplate>
            </ContentControl>
        </Border>
    </DockPanel>
</UserControl>
