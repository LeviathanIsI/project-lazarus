<UserControl x:Class="Lazarus.App.Desktop.Views.ModelConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Lazarus.App.Desktop.Converters"
             xmlns:behaviors="clr-namespace:Lazarus.App.Desktop.Behaviors">
    
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:FileSizeConverter x:Key="FileSizeConverter" />
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Model Configuration" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"
                   Foreground="{DynamicResource PrimaryForegroundBrush}" />

        <!-- Asset Selection Section -->
        <Border Grid.Row="1" 
                Background="{DynamicResource CardBackgroundBrush}" 
                Padding="25" 
                Margin="0,0,0,20"
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="15" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <!-- Section Header -->
                <TextBlock Grid.Row="0"
                           Text="Asset Configuration"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryForegroundBrush}"
                           Margin="0,0,0,5" />
                
                <!-- Asset Selection Grid -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="15" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Base Model Selection -->
                    <StackPanel Grid.Row="0" Grid.Column="0">
                        <TextBlock Text="Base Model"
                                   FontWeight="Medium"
                                   Margin="0,0,0,5"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}" />
                        <ComboBox ItemsSource="{Binding BaseModels}"
                                  SelectedItem="{Binding SelectedBaseModel}"
                                  Style="{DynamicResource LazarusComboBoxStyle}"
                                  MinHeight="35">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="{Binding Name}" FontWeight="Medium" />
                                        <TextBlock Text="{Binding ParameterCount}" 
                                                   FontSize="11" 
                                                   Opacity="0.7" 
                                                   Margin="0,2,0,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>

                    <!-- Embeddings Selection -->
                    <StackPanel Grid.Row="0" Grid.Column="2">
                        <TextBlock Text="Embeddings Model"
                                   FontWeight="Medium"
                                   Margin="0,0,0,5"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}" />
                        <ComboBox ItemsSource="{Binding SelectableEmbeddings}"
                                  Style="{DynamicResource MultiSelectComboBoxStyle}"
                                  ItemContainerStyle="{DynamicResource MultiSelectComboBoxItemStyle}"
                                  MinHeight="35"
                                  MaxHeight="100">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                              Command="{Binding DataContext.ToggleEmbeddingSelectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              CommandParameter="{Binding}"
                                              Style="{DynamicResource MultiSelectCheckBoxStyle}"
                                              Margin="4,2">
                                        <CheckBox.Content>
                                            <StackPanel Orientation="Vertical" Margin="8,4">
                                                <TextBlock Text="{Binding Name}" FontWeight="Medium" />
                                                <TextBlock Text="{Binding Architecture}" 
                                                           FontSize="11" 
                                                           Opacity="0.7" 
                                                           Margin="0,2,0,0" />
                                            </StackPanel>
                                        </CheckBox.Content>
                                    </CheckBox>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>

                    <!-- LoRA Adapter Selection -->
                    <StackPanel Grid.Row="2" Grid.Column="0">
                        <TextBlock Text="LoRA Adapter"
                                   FontWeight="Medium"
                                   Margin="0,0,0,5"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}" />
                        <ComboBox ItemsSource="{Binding SelectableLoRAAdapters}"
                                  Style="{DynamicResource MultiSelectComboBoxStyle}"
                                  ItemContainerStyle="{DynamicResource MultiSelectComboBoxItemStyle}"
                                  MinHeight="35"
                                  MaxHeight="100">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                              Command="{Binding DataContext.ToggleLoRAAdapterSelectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              CommandParameter="{Binding}"
                                              Style="{DynamicResource MultiSelectCheckBoxStyle}"
                                              Margin="4,2">
                                        <CheckBox.Content>
                                            <StackPanel Orientation="Vertical" Margin="8,4">
                                                <TextBlock Text="{Binding Name}" FontWeight="Medium" />
                                                <TextBlock Text="{Binding Description}" 
                                                           FontSize="11" 
                                                           Opacity="0.7" 
                                                           Margin="0,2,0,0"
                                                           TextTrimming="CharacterEllipsis" />
                                            </StackPanel>
                                        </CheckBox.Content>
                                    </CheckBox>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>

                    <!-- Tokenizer Selection -->
                    <StackPanel Grid.Row="2" Grid.Column="2">
                        <TextBlock Text="Tokenizer"
                                   FontWeight="Medium"
                                   Margin="0,0,0,5"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}" />
                        <ComboBox ItemsSource="{Binding Tokenizers}"
                                  SelectedItem="{Binding SelectedTokenizer}"
                                  Style="{DynamicResource LazarusComboBoxStyle}"
                                  MinHeight="35">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="{Binding Name}" FontWeight="Medium" />
                                        <TextBlock Text="{Binding Architecture}" 
                                                   FontSize="11" 
                                                   Opacity="0.7" 
                                                   Margin="0,2,0,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>

                    <!-- Status and Actions -->
                    <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Left">
                        <Button Command="{Binding RefreshAssetsCommand}"
                                Content="Refresh Asset Library" 
                                Style="{DynamicResource ActionButtonStyle}"
                                Padding="12,8"
                                Margin="0,0,10,0" />
                        
                        <Button Command="{Binding ClearAllSelectionsCommand}"
                                Content="Clear Selections" 
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Padding="12,8"
                                Margin="0,0,10,0"
                                ToolTip="Clear all current asset selections" />
                        
                        <!-- Loading Indicator -->
                        <Grid Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}}"
                              Margin="0,0,10,0">
                            <Border Background="{DynamicResource TertiaryBackgroundBrush}"
                                    CornerRadius="4"
                                    Padding="8,6">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="12" Height="12" 
                                             Fill="{DynamicResource AccentBrush}"
                                             Margin="0,0,6,0">
                                        <Ellipse.RenderTransform>
                                            <RotateTransform x:Name="LoadingRotation" />
                                        </Ellipse.RenderTransform>
                                        <Ellipse.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard RepeatBehavior="Forever">
                                                        <DoubleAnimation Storyboard.TargetName="LoadingRotation"
                                                                         Storyboard.TargetProperty="Angle"
                                                                         From="0" To="360" Duration="0:0:1" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Ellipse.Triggers>
                                    </Ellipse>
                                    <TextBlock Text="Scanning..."
                                               Foreground="{DynamicResource AccentForegroundBrush}"
                                               FontSize="12"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </Border>
                        </Grid>
                        <TextBlock Text="{Binding AssetScanStatus}" 
                                   VerticalAlignment="Center"
                                   Margin="15,0,0,0"
                                   FontStyle="Italic"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                                   Visibility="{Binding HasScanStatus, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </StackPanel>

                    <!-- Enhanced Status Display -->
                    <Grid Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="3">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        
                        <!-- Asset Discovery Status -->
                        <Border Grid.Row="0"
                                Background="{DynamicResource InfoBackgroundBrush}"
                                Padding="12,8"
                                Margin="0,0,0,8"
                                CornerRadius="4"
                                Visibility="{Binding HasDiscoveryStatus, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Ellipse Grid.Column="0"
                                         Width="8" Height="8"
                                         Fill="{DynamicResource SuccessBrush}"
                                         Margin="0,0,8,0"
                                         VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1"
                                           Text="{Binding DiscoveryStatusMessage}"
                                           Foreground="{DynamicResource InfoForegroundBrush}"
                                           FontSize="12"
                                           TextWrapping="Wrap" />
                            </Grid>
                        </Border>
                        
                        <!-- Compatibility Warning -->
                        <Border Grid.Row="1"
                                Background="{DynamicResource InfoBackgroundBrush}"
                                Padding="12,8"
                                CornerRadius="4"
                                Visibility="{Binding HasCompatibilityWarning, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Path Grid.Column="0"
                                      Data="M12 2L2 22h20L12 2zm0 15h-1v1h1v-1zm0-10v7h-1V7h1z"
                                      Fill="{DynamicResource WarningBrush}"
                                      Width="12" Height="12"
                                      Stretch="Uniform"
                                      Margin="0,0,8,0"
                                      VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1"
                                           Text="{Binding CompatibilityMessage}"
                                           Foreground="{DynamicResource InfoForegroundBrush}"
                                           FontSize="12"
                                           TextWrapping="Wrap" />
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Border>


        <!-- Selected Assets Registry - SELECTION DATA ONLY (dropdown sources remain intact) -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding SelectedAssets}"
                  SelectedItem="{Binding SelectedAsset}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  RowHeight="48"
                  FontSize="14"
                  Style="{DynamicResource EnhancedDataGridStyle}">
            <DataGrid.Resources>
                <!-- Enhanced DataGrid Style with Generous Spacing -->
                <Style x:Key="EnhancedDataGridStyle" TargetType="DataGrid">
                    <Setter Property="Background" Value="{DynamicResource SurfaceBackgroundBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                    <Setter Property="BorderThickness" Value="1" />
                    <Setter Property="RowBackground" Value="{DynamicResource SurfaceBackgroundBrush}" />
                    <Setter Property="AlternatingRowBackground" Value="{DynamicResource SecondaryBackgroundBrush}" />
                    <Setter Property="GridLinesVisibility" Value="Horizontal" />
                    <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BorderBrush}" />
                    <Setter Property="RowHeight" Value="48" />
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
                    <Setter Property="HeadersVisibility" Value="Column" />
                    <Setter Property="CanUserResizeRows" Value="False" />
                    <Setter Property="CanUserReorderColumns" Value="False" />
                    <Setter Property="SelectionMode" Value="Single" />
                    <Setter Property="SelectionUnit" Value="FullRow" />
                </Style>

                <!-- Enhanced Column Header Style with High Contrast -->
                <Style x:Key="EnhancedColumnHeaderStyle" TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="Padding" Value="16,12" />
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                    <Setter Property="BorderThickness" Value="0,0,1,0" />
                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="MinHeight" Value="44" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridColumnHeader">
                                <Grid>
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                    </Border>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource AccentHoverBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

                <!-- Enhanced Cell Style with Professional Padding -->
                <Style x:Key="EnhancedCellStyle" TargetType="DataGridCell">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="16,8" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                    <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource FocusBorderBrush}" />
                                        <Setter Property="BorderThickness" Value="2" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

                <!-- Action Button Style for Data Grid -->
                <Style x:Key="DataGridActionButtonStyle" TargetType="Button">
                    <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                    <Setter Property="BorderThickness" Value="1" />
                    <Setter Property="Padding" Value="10,6" />
                    <Setter Property="Margin" Value="4,2" />
                    <Setter Property="FontSize" Value="12" />
                    <Setter Property="MinWidth" Value="70" />
                    <Setter Property="MinHeight" Value="30" />
                    <Setter Property="Height" Value="32" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="SnapsToDevicePixels" Value="True" />
                </Style>

                <!-- Danger Button Style for Data Grid -->
                <Style x:Key="DataGridDangerButtonStyle" TargetType="Button">
                    <Setter Property="Background" Value="{DynamicResource DangerBrush}" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="BorderBrush" Value="{DynamicResource DangerBrush}" />
                    <Setter Property="BorderThickness" Value="1" />
                    <Setter Property="Padding" Value="10,6" />
                    <Setter Property="Margin" Value="4,2" />
                    <Setter Property="FontSize" Value="12" />
                    <Setter Property="MinWidth" Value="70" />
                    <Setter Property="MinHeight" Value="30" />
                    <Setter Property="Height" Value="32" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="SnapsToDevicePixels" Value="True" />
                </Style>
            </DataGrid.Resources>

            <DataGrid.ColumnHeaderStyle>
                <StaticResource ResourceKey="EnhancedColumnHeaderStyle" />
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.CellStyle>
                <StaticResource ResourceKey="EnhancedCellStyle" />
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Asset Name" 
                                    Binding="{Binding Name}" 
                                    Width="2*" 
                                    MinWidth="200"
                                    MaxWidth="350" />
                <DataGridTextColumn Header="Type" 
                                    Binding="{Binding AssetType}" 
                                    Width="120" />
                <DataGridTextColumn Header="Status" 
                                    Binding="{Binding Status}" 
                                    Width="110" />
                <DataGridTextColumn Header="Architecture" 
                                    Binding="{Binding Architecture}" 
                                    Width="130" />
                <DataGridTextColumn Header="Parameters" 
                                    Binding="{Binding ParameterCount}" 
                                    Width="120" />
                <DataGridTextColumn Header="VRAM (GB)" 
                                    Binding="{Binding VramEstimateGb, StringFormat={}{0:F1}}" 
                                    Width="100" />
                <DataGridTextColumn Header="File Size" 
                                    Binding="{Binding FileSizeBytes, Converter={StaticResource FileSizeConverter}}" 
                                    Width="110" />
                <DataGridTemplateColumn Header="Actions" Width="100" MinWidth="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" 
                                        HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        Margin="8,4">
                                <Button Content="Remove" 
                                        Command="{Binding DataContext.RemoveAssetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource DataGridDangerButtonStyle}"
                                        ToolTip="Remove from selections (asset remains available in dropdowns)" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>