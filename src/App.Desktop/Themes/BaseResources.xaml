<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:System="clr-namespace:System;assembly=mscorlib">

    <!-- Fundamental colors used across themes -->
    <Color x:Key="BackgroundColor">#0A0A0F</Color>
    <Color x:Key="CardBackgroundColor">#0A0A0F</Color>
    <Color x:Key="CardOverlayColor">#1A1A1A</Color>
    <!-- Surface: neutral container background consistent with dark glass theme -->
    <Color x:Key="SurfaceColor">{StaticResource CardOverlayColor}</Color>
    <Color x:Key="TextPrimaryColor">#FFFFFF</Color>
    <Color x:Key="TextSecondaryColor">#A0A0A0</Color>
    <Color x:Key="GlassBorderColor">#19FFFFFF</Color>
    <Color x:Key="ShadowColor">#33000000</Color>

    <!-- Fundamental brushes -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackgroundColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="GlassBorderBrush" Color="{StaticResource GlassBorderColor}"/>

    <!-- Additional safe defaults used by views -->
    <!-- DarkGlassBrush: darker card background for emphasis bubbles -->
    <SolidColorBrush x:Key="DarkGlassBrush" Color="#141414"/>

    <!-- Secondary button interaction brushes (dark theme) -->
    <SolidColorBrush x:Key="SecondaryButtonHoverBrush" Color="#222222"/>
    <SolidColorBrush x:Key="SecondaryButtonPressedBrush" Color="#141414"/>

    <!-- Shared gradient/overlay brushes used across themes -->
    <LinearGradientBrush x:Key="GlassCardBrush" StartPoint="0,0" EndPoint="0,1" Opacity="0.6">
        <GradientStop Color="#1A1A1A1A" Offset="0"/>
        <GradientStop Color="#0D1A1A1A" Offset="1"/>
    </LinearGradientBrush>

    <!-- Typography (shared) -->
    <FontFamily x:Key="SystemFont">Segoe UI</FontFamily>
    <FontFamily x:Key="MonoFont">Cascadia Code, Consolas, Courier New</FontFamily>

    <System:Double x:Key="FontSize_H1">32</System:Double>
    <System:Double x:Key="FontSize_H2">24</System:Double>
    <System:Double x:Key="FontSize_H3">18</System:Double>
    <System:Double x:Key="FontSize_Body">14</System:Double>
    <System:Double x:Key="FontSize_Small">12</System:Double>
    <System:Double x:Key="FontSize_Mono">13</System:Double>

    <LinearGradientBrush x:Key="GlassBorderGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#19FFFFFF" Offset="0"/>
        <GradientStop Color="#00FFFFFF" Offset="1"/>
    </LinearGradientBrush>

    <!-- Base Secondary Button Style (no theme-specific dependencies) -->
    <Style x:Key="SecondaryGlassButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource GlassCardBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource GlassBorderGradientBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="FontFamily" Value="{StaticResource SystemFont}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSize_Body}"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource SecondaryButtonHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource SecondaryButtonPressedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Colored action button that darkens on hover/press but preserves base color -->
    <Style x:Key="ColoredActionButton" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <!-- Base fill comes from Background set on the Button instance -->
                        <Border x:Name="Base" Background="{TemplateBinding Background}" CornerRadius="6"/>
                        <!-- Overlay to darken on hover/press without losing color -->
                        <Border x:Name="Overlay" Background="#000000" CornerRadius="6" Opacity="0"/>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Overlay" Property="Opacity" Value="0.15"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Overlay" Property="Opacity" Value="0.28"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Base" Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
