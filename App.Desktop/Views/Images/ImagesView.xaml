<UserControl x:Class="Lazarus.Desktop.Views.Images.ImagesView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:local="clr-namespace:Lazarus.Desktop.Views.Images"
			 mc:Ignorable="d">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<!-- tabs -->
			<RowDefinition Height="*"/>
			<!-- content -->
		</Grid.RowDefinitions>
		<!-- SubTab Buttons -->
		<StackPanel Grid.Row="0"
					Orientation="Horizontal"
					Background="{DynamicResource TertiaryDarkBrush}"
					Margin="8">
			<Button x:Name="Text2ImageTab"
					Content="Text2Image"
					Tag="Text2Image"
					Style="{StaticResource SubTabButtonStyle}"
					Click="SubTabButton_Click"
					Margin="4,0"/>
			<Button x:Name="Image2ImageTab"
					Content="Image2Image"
					Tag="Image2Image"
					Style="{StaticResource SubTabButtonStyle}"
					Click="SubTabButton_Click"
					Margin="4,0"/>
			<Button x:Name="InpaintingTab"
					Content="Inpainting"
					Tag="Inpainting"
					Style="{StaticResource SubTabButtonStyle}"
					Click="SubTabButton_Click"
					Margin="4,0"/>
			<Button x:Name="UpscalingTab"
					Content="Upscaling"
					Tag="Upscaling"
					Style="{StaticResource SubTabButtonStyle}"
					Click="SubTabButton_Click"
					Margin="4,0"/>
		</StackPanel>
		<!-- Content Panels -->
		<Grid Grid.Row="1">
			<local:Text2ImageView x:Name="Text2ImageContent"
								 Visibility="Visible"/>
			<local:Image2ImageView x:Name="Image2ImageContent"
							 Visibility="Collapsed"/>
			<local:InpaintingView x:Name="InpaintingContent"
								   Visibility="Collapsed"/>
			<local:UpscalingView x:Name="UpscalingContent"
							Visibility="Collapsed"/>
		</Grid>
	</Grid>
</UserControl>