<UserControl x:Class="Lazarus.Desktop.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer x:Name="MessageScroller" Grid.Row="0" VerticalScrollBarVisibility="Auto" 
                      Background="#1a1a1a" Padding="8">
            <ItemsControl ItemsSource="{Binding Messages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,6" Padding="12,8" CornerRadius="12"
                                Background="{Binding Role, Converter={StaticResource RoleToBrush}}"
                                HorizontalAlignment="{Binding Role, Converter={StaticResource RoleToAlignment}}"
                                MaxWidth="400">
                            <StackPanel>
                                <TextBlock Text="{Binding Role}" FontSize="10" Opacity="0.7" 
                                           Margin="0,0,0,4" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Text}" TextWrapping="Wrap" 
                                           Foreground="White" FontFamily="Consolas"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="1" Background="#2d2d2d" CornerRadius="8" Padding="8" Margin="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="InputBox" Grid.Column="0" 
                         Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" 
                         AcceptsReturn="True" MinHeight="36" MaxHeight="120"
                         Background="Transparent" Foreground="White" BorderThickness="0"
                         VerticalAlignment="Center" FontFamily="Consolas"
                         TextWrapping="Wrap" VerticalScrollBarVisibility="Auto">
                    <TextBox.InputBindings>
                        <KeyBinding Command="{Binding SendCommand}" Key="Return" />
                    </TextBox.InputBindings>
                </TextBox>
                <Button Grid.Column="1" Content="Send" 
                        Command="{Binding SendCommand}" 
                        Margin="8,0,0,0" MinWidth="70" Height="36"
                        Foreground="White" BorderThickness="0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#6b46c1"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Input, Converter={StaticResource StringToBool}}" Value="True">
                                    <Setter Property="Background" Value="#8b5cf6"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>