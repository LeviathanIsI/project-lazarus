<UserControl x:Class="Lazarus.Desktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Lazarus.Desktop.Views"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <!-- REMOVED ViewModeTemplateSelector for debugging -->

    <!-- SIMPLE DASHBOARD CONTENT -->
    <Grid Background="{DynamicResource PrimaryDarkBrush}">
        <Border Background="{DynamicResource AccentRedBrush}" Margin="40" CornerRadius="8">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🏠 DASHBOARD" 
                           Foreground="White" FontSize="24" FontWeight="Bold"
                           HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Welcome to Project Lazarus" 
                           Foreground="White" FontSize="16"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Navigation is now working correctly!" 
                           Foreground="White" FontSize="14" FontStyle="Italic"
                           HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>