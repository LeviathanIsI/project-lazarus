<UserControl x:Class="Lazarus.Desktop.Views.JobsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Lazarus.Desktop.Views"
             Background="{DynamicResource PrimaryDarkBrush}">

    <!-- Jobs Content - use global ViewModeTemplateSelector so mode changes retheme consistently -->
    <ContentControl x:Name="ModeAwareContent" Content="{Binding}">
        <ContentControl.ContentTemplateSelector>
            <StaticResource ResourceKey="ViewModeTemplateSelector"/>
        </ContentControl.ContentTemplateSelector>
    </ContentControl>
</UserControl>