<UserControl x:Class="Lazarus.Desktop.Views.RunnerManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Lazarus.Desktop.Views"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <!-- Use global selector so mode changes live-update consistently -->
    <ContentControl Content="{Binding}">
        <ContentControl.ContentTemplateSelector>
            <StaticResource ResourceKey="ViewModeTemplateSelector"/>
        </ContentControl.ContentTemplateSelector>
    </ContentControl>
</UserControl>