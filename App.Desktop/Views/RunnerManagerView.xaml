<UserControl x:Class="Lazarus.Desktop.Views.RunnerManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Lazarus.Desktop.Views"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <!-- ViewMode Template Selector - PROGRESSIVE DISCLOSURE RUNNER MANAGER -->
        <!-- Novice: Simple runner selection + basic status -->
        <!-- Enthusiast: Runner switching + detailed status + controls -->
        <!-- Developer: Full runner management + logs + process control + health monitoring -->
        <local:ViewModeTemplateSelector x:Key="RunnerManagerTemplateSelector"/>
    </UserControl.Resources>

    <!-- Runner Manager Content Selector - Template chosen by ViewMode -->
    <ContentPresenter Content="{Binding}"
                      ContentTemplateSelector="{StaticResource RunnerManagerTemplateSelector}">
        <!-- Local fallback if selector returns null -->
        <ContentPresenter.ContentTemplate>
            <DataTemplate>
                <Border Padding="16" BorderThickness="2"
                        BorderBrush="{DynamicResource AccentRedBrush}">
                    <StackPanel>
                        <TextBlock Text="[FALLBACK] No ViewMode template selected"
                                   FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="This is a diagnostic default inside the view."
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ContentPresenter.ContentTemplate>
    </ContentPresenter>
</UserControl>